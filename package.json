{
  "scripts": {
    "postinstall": "cd client && npm install",
    "heroku-postbuild": "cd client && npm run build"
  },
  "engines": {
    "node": "18.*.*",
    "npm": "9.*.*"
  },
  "cacheDirectories": [
    "client/node_modules",
    ".cache/Cypress"
  ],
  "dependencies": {
    "@apollo/client": "^3.8.7",
    "dotenv": "^16.3.1",
    "graphql": "^16.8.1",
    "graphql-tag": "^2.12.6"
  }
}
